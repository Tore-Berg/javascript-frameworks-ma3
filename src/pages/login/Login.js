import React from "react";
import Heading from "../../components/Heading";
import { StyledLogin } from "./Login.Styled";

const Login = () => {
  return (
    <StyledLogin>
      <Heading title={`Sorry, I'll work more on this later.`} />
    </StyledLogin>
  );
};

export default Login;
